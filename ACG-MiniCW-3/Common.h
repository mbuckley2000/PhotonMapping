#pragma once

/**
 * Returns the larger of the two float arguments
 */
float max(float a, float b);

/**
 * Returns the smaller of the two float arguments
 */
float min(float a, float b);


float random(float min, float max);